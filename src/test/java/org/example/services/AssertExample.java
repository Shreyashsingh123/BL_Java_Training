package org.example.services;

public class AssertExample {
//    Assertion is validating actual result with expected result
    //Assertions is  aclass
//    Assertion.assertArrayEquals(expectedIntarray,actualintArray)
//    used to compare two integer array values
//    Assertion.assertSame() used to compare two objects references not value  , if we use new keyword then it will fail test case
//     Assertion.assertTrue(value)    if value is true then test case passs else fails same for Assertion.assertFalse;
// Assertions.assertIterableEquals(list1,list2)   used to compare two list values with their order   similaly for hashmap,hashset,all data structures which are iterable
//    Assertions.assertThrows(RuntimeException.class,Executable)  here executable si avariable and runtimeException.class is a runtime exception that can occur in that class

}
